Aqui está o conteúdo atualizado e reorganizado para o formato **GitHub Markdown**, integrando a seção de **Vetores (Arrays)** de forma lógica entre as estruturas de repetição e as funções.

---

# 🚀 JavaScript: Fluxo, Laços e Vetores

**Assunto:** Estruturas de decisão, repetição, manipulação de coleções (Vetores) e modularização.

---

## 1. Controle de Fluxo (`if`, `else`, `switch`)

Permitem que o programa execute diferentes blocos de código com base em condições lógicas.

### Exemplo: Condicionais Compostas

```javascript
const nota = 8.5;

if (nota >= 9) {
    console.log("Desempenho Excelente!");
} else if (nota >= 7) {
    console.log("Aprovado");
} else {
    console.log("Reprovado");
}

```

---

## 2. Vetores (Arrays)

Um **Vetor** é uma estrutura de dados que permite armazenar múltiplos valores em uma única variável. No JavaScript, os vetores são dinâmicos e indexados a partir do **0**.

### Operações Básicas

```javascript
// Criando um vetor de frutas
const frutas = ["Maçã", "Banana", "Morango"];

console.log(frutas[0]);      // Acesso: Maçã
console.log(frutas.length);  // Tamanho: 3

frutas.push("Uva");          // Adiciona ao final
frutas[1] = "Pêra";          // Altera o valor no índice 1

```

---

## 3. Laços de Repetição (`for`, `while`)

Utilizados para repetir ações ou percorrer os elementos de um **Vetor**.

### Percorrendo um Vetor com `for`

```javascript
const nomes = ["Ana", "Bia", "Caio", "Duda"];

for (let i = 0; i < nomes.length; i++) {
    console.log(`Posição ${i} - Nome: ${nomes[i]}`);
}

```

### Laço `while`

Executa enquanto uma condição for verdadeira.

```javascript
let bateria = 3;
while (bateria > 0) {
    console.log(`Carga em ${bateria}%...`);
    bateria--;
}
console.log("Celular desligado.");

```

---

## 4. Funções: Parâmetros e Retorno

Funções permitem isolar e reutilizar lógica de programação. Elas podem receber valores (parâmetros) e devolver resultados (`return`).

```javascript
function calcularMediaVetor(numeros) {
    let soma = 0;
    for (let n of numeros) {
        soma += n;
    }
    return soma / numeros.length;
}

const mediaFinal = calcularMediaVetor([10, 8, 9, 7]);
console.log(`A média é: ${mediaFinal}`);

```

---

## 5. Funções Anônimas e Callbacks

Funções podem ser tratadas como variáveis. Um **Callback** é uma função passada para outra função para ser executada em um momento específico.

### Exemplo: Callback em Vetores (`forEach`)

```javascript
const precos = [10, 20, 30];

// O método forEach executa a função de callback para cada item do vetor
precos.forEach((valor) => {
    console.log(`Preço com imposto: R$ ${valor * 1.1}`);
});

```

---

## 🛠️ Atividades de Fixação

### Exercício 1: Buscador em Vetor

Crie um vetor com 5 nomes de cidades. Crie uma função que receba o vetor e um nome de cidade. A função deve retornar `true` se a cidade existir no vetor e `false` caso contrário.

### Exercício 2: Somador de Ímpares

Crie um vetor de números de 1 a 10. Utilize um laço `for` para percorrer o vetor e calcular a soma apenas dos números que forem **ímpares**.

### Exercício 3: Calculadora com Callback

Crie uma função chamada `executarOperacao(a, b, callback)`.

1. Crie um callback para **soma**.
2. Crie um callback para **multiplicação**.
3. Chame a função `executarOperacao` para mostrar os resultados no console.

### Exercício 4: Filtragem de Lista (Maioridade)

Dado o array de idades `[12, 18, 25, 10, 33, 16]`, utilize um laço `for` ou o método `forEach` para criar um novo vetor contendo apenas as idades maiores ou iguais a 18.

