Perfeito. Aqui está o conteúdo técnico estruturado para a sua aula de JavaScript, focado em lógica de controle e o poder das funções, formatado para documentação no GitHub.

---

# JS: Fluxo, Laços e Funções

**Assunto:** Estruturas de decisão, repetição e modularização com funções.

---

## 1. Controle de Fluxo (`if`, `else`, `switch`)

Permitem que o programa execute diferentes blocos de código com base em condições booleanas.

### Exemplo: Condicionais Compostas

```javascript
const nota = 8.5;

if (nota >= 9) {
    console.log("Desempenho Excelente!");
} else if (nota >= 7) {
    console.log("Aprovado");
} else {
    console.log("Reprovado");
}

```

### Exemplo: Switch Case

Ideal para quando temos múltiplas opções fixas para uma única variável.

```javascript
const plano = "Premium";

switch (plano) {
    case "Basico":
        console.log("Acesso limitado.");
        break;
    case "Premium":
        console.log("Acesso total e HD.");
        break;
    default:
        console.log("Plano inválido.");
}

```

---

## 2. Laços de Repetição (`for`, `while`)

Utilizados para executar o mesmo bloco de código várias vezes.

### Laço `for`

Geralmente usado quando sabemos o número de repetições.

```javascript
// Contagem de 1 a 5
for (let i = 1; i <= 5; i++) {
    console.log("Passo: " + i);
}

```

### Laço `while`

Usado quando a repetição depende de uma condição que pode mudar a qualquer momento.

```javascript
let bateria = 10;
while (bateria > 0) {
    console.log(`Celular ligado... Carga em ${bateria}%`);
    bateria--;
}
console.log("Celular desligou.");

```

---

## 3. Funções: Parâmetros e Retorno

Funções são blocos de código reutilizáveis.

* **Parâmetros:** São as entradas da função.
* **Retorno:** É a saída que a função devolve para quem a chamou.

```javascript
function calcularDesconto(preco, percentual) {
    const desconto = preco * (percentual / 100);
    return preco - desconto;
}

const valorFinal = calcularDesconto(100, 15); // Retorna 85
console.log(`Total com desconto: R$ ${valorFinal}`);

```

---

## 4. Funções Anônimas e Callbacks

Em JavaScript, funções são "cidadãs de primeira classe", o que significa que podem ser armazenadas em variáveis e passadas como argumentos.

### Função Anônima (Arrow Function)

```javascript
const dobrar = (n) => n * 2;
console.log(dobrar(5)); // 10

```

### Callbacks

Um **Callback** é uma função passada como parâmetro para ser executada mais tarde.

```javascript
function processarDados(nome, callback) {
    console.log("Iniciando processamento...");
    callback(nome);
}

processarDados("GitHub", (n) => {
    console.log("Finalizado para o usuário: " + n);
});

```

---

## 🛠️ Atividades de Fixação

### Exercício 1: Verificador de Paridade

Crie uma função que receba um número como parâmetro e retorne uma String dizendo se o número é "Par" ou "Ímpar". Use o operador de módulo `%`.

### Exercício 2: Tabuada Dinâmica

Crie uma função que receba um número e, utilizando um laço `for`, imprima no console a tabuada desse número de 1 a 10.

### Exercício 3: Calculadora com Callback

Crie uma função chamada `executarOperacao`. Ela deve receber dois números e uma função de callback.

1. Crie uma função de callback para **soma**.
2. Crie uma função de callback para **multiplicação**.
3. Teste a `executarOperacao` passando os números e os diferentes callbacks.


