
# 🚀 JavaScript Profundo: Fluxo, Vetores e Funções

**Assunto:** Controle de decisão, laços de repetição, manipulação de arrays e funções avançadas.

---

## 1. Controle de Fluxo: A Tomada de Decisão

O controle de fluxo permite que o software execute caminhos diferentes baseados em condições lógicas (booleanas).

### 1.1 Condicionais (`if`, `else if`, `else`)

Utilizamos operadores de comparação (`==`, `===`, `!=`, `!==`, `>`, `<`) e operadores lógicos (`&&`, `||`, `!`).

```javascript
const idade = 20;
const temTitulo = true;

if (idade >= 18 && temTitulo) {
    console.log("Pode votar.");
} else if (idade >= 16) {
    console.log("Voto opcional.");
} else {
    console.log("Não pode votar.");
}

```

### 1.2 Seleção Múltipla (`switch`)

Ideal para verificar uma única variável contra múltiplos valores constantes.

```javascript
const corSemaforo = "Amarelo";

switch (corSemaforo.toLowerCase()) {
    case "verde":
        console.log("Siga");
        break;
    case "amarelo":
        console.log("Atenção");
        break;
    case "vermelho":
        console.log("Pare");
        break;
    default:
        console.log("Sinal com defeito.");
}

```

---

## 2. Vetores (Arrays): Coleções de Dados

Um **Vetor** é uma lista ordenada de elementos. No JavaScript, os vetores são dinâmicos (podem crescer ou diminuir) e podem conter diferentes tipos de dados simultaneamente.

### 2.1 Manipulação de Vetores

* **Acesso:** O primeiro elemento está sempre no índice `0`.
* **Métodos Principais:**
* `.push()`: Adiciona ao final.
* `.pop()`: Remove o último.
* `.unshift()`: Adiciona no início.
* `.shift()`: Remove o primeiro.
* `.length`: Propriedade que retorna a quantidade de elementos.



```javascript
let tecnologias = ["HTML", "CSS"];
tecnologias.push("JS");        // ["HTML", "CSS", "JS"]
tecnologias[1] = "SASS";       // ["HTML", "SASS", "JS"]

console.log(tecnologias[tecnologias.length - 1]); // Pega o último item

```

---

## 3. Laços de Repetição: Automação de Tarefas

Laços (Loops) evitam a repetição manual de código, sendo essenciais para percorrer vetores.

### 3.1 O clássico `for`

Possui três partes: `inicialização`, `condição` e `incremento`.

```javascript
const notas = [10, 8, 5, 9];
let soma = 0;

for (let i = 0; i < notas.length; i++) {
    soma += notas[i];
}
console.log(`Média: ${soma / notas.length}`);

```

### 3.2 O Moderno `for...of`

Forma simplificada de percorrer todos os valores de um vetor sem precisar de um contador.

```javascript
const precos = [50, 100, 150];
for (let preco of precos) {
    console.log(`Preço com taxa: ${preco * 1.05}`);
}

```

---

## 4. Funções: Organização e Reuso

Funções encapsulam blocos de código para serem executados sob demanda.

### 4.1 Parâmetros e Retorno

* **Parâmetros:** Variáveis locais que recebem valores na chamada da função.
* **Return:** Finaliza a função e envia um valor de volta para quem a chamou.

```javascript
function formatarMoeda(valor, simbolo = "R$") {
    return `${simbolo} ${valor.toFixed(2)}`;
}

console.log(formatarMoeda(1500.5)); // R$ 1500.50

```

---

## 5. Funções Anônimas, Arrow Functions e Callbacks

Em JavaScript, funções podem ser passadas como argumentos para outras funções.

### 5.1 Arrow Functions (`=>`)

Sintaxe curta e moderna para escrever funções anônimas.

```javascript
const calcularArea = (base, altura) => (base * altura) / 2;

```

### 5.2 Callbacks

Um **Callback** permite que uma função controle *quando* outra função será executada.

```javascript
// Função que aceita um callback
function baixarArquivo(nome, aoFinalizar) {
    console.log(`Baixando ${nome}...`);
    // Simulando que o download terminou
    aoFinalizar();
}

baixarArquivo("aula.pdf", () => {
    console.log("Download concluído com sucesso!");
});

```

---

## 🛠️ Atividades de Fixação (Desafios)

### Exercício 1: Analisador de Números

Crie um vetor com 6 números inteiros aleatórios.

1. Percorra o vetor e mostre a soma de todos os números.
2. Identifique e mostre qual é o **maior** número presente no vetor.

### Exercício 2: Lista de Compras (CRUD Básico)

Crie um vetor chamado `carrinho`.

1. Crie uma função `adicionarProduto(nome)` que insere o item no vetor apenas se ele não existir (use `.includes()`).
2. Crie uma função `removerUltimo()` que remove o último item e avisa qual foi removido.

### Exercício 3: Transformador de Dados (Callback)

Crie uma função chamada `transformarLista(lista, callback)`.

1. Passe um vetor de nomes como `["pedro", "maria", "joão"]`.
2. O callback deve transformar o nome para **MAIÚSCULAS**.
3. A função deve retornar um novo vetor com os nomes transformados.

### Exercício 4: Verificador de Login

Crie um vetor de objetos chamado `usuarios`, onde cada objeto tem `{login: "admin", senha: "123"}`.

* Crie uma função que receba login e senha e verifique se as credenciais batem com algum usuário do vetor. Retorne "Acesso Permitido" ou "Acesso Negado".

